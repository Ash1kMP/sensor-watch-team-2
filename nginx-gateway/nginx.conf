# worker_processes 1;

# events {
#     worker_connections 1024;
# }

# http {
#     include       mime.types;
#     default_type  application/octet-stream;

#     sendfile        on;
#     keepalive_timeout 65;

#     server {
#         listen 80;
#         server_name localhost;

#         # --- WebSocket to consumer-service-fe ---
#         location /ws {
#         proxy_pass http://consumer-service-fe:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         }

#         # --- Auth API ---
#         location /api/auth/ {
#         proxy_pass http://auth-service:3002/api/auth/;
#         }

#         # (optional) other APIs in future under /api/…

#         # --- Angular app ---
#         root /usr/share/nginx/html;
#         index index.html;

#         location / {
#         try_files $uri $uri/ /index.html;
#         }
# }
#         location /ws {
#             proxy_pass http://consumer-service-fe:3000;
#             proxy_http_version 1.1;
#             proxy_set_header Upgrade $http_upgrade;
#             proxy_set_header Connection "Upgrade";
#             proxy_set_header Host $host;
#             proxy_read_timeout 3600;
# }
#         location /queue {
#             proxy_pass http://queue-service:4000;
#         }

#         location /discovery {
#             proxy_pass http://discovery-service:5000;
#         }

#         location / {
#             root   /usr/share/nginx/html;
#             index  index.html index.htm;
#         }
#     }
# }

# worker_processes 1;
# events { worker_connections 1024; }
# http {
#   include mime.types; sendfile on; keepalive_timeout 65;

#   server {
#     listen 80; server_name _;

#     # WebSocket to consumer-service-fe
#     location /ws {
#       proxy_pass http://consumer-service-fe:3000;
#       proxy_http_version 1.1;
#       proxy_set_header Upgrade $http_upgrade;
#       proxy_set_header Connection "upgrade";
#       proxy_set_header Host $host;
#     }

#     # Auth API to auth-service
#     location /api/auth/ {
#       proxy_pass http://auth-service:3002/api/auth/;
#     }

#     # Angular app (dev server at 4200)
#      location / {
#     proxy_pass http://frontend-dashboard:4200;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection $connection_upgrade;
#   }
#     }
#   }
# }
# nginx-gateway/nginx.conf

user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;

events { worker_connections 1024; }

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;

  log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';
  access_log  /var/log/nginx/access.log  main;

  sendfile        on;
  keepalive_timeout  65;

  # --------- PROXY SERVER BLOCK LIVES INSIDE http{} ----------
  server {
    listen 80 default_server;
    server_name _;

    # Angular dev server
    location / {
      proxy_pass http://frontend-dashboard:4200;
      proxy_http_version 1.1;

      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;

      proxy_set_header Connection "keep-alive";
    }

    # WebSocket to consumer-service-fe
    location /ws {
      proxy_pass http://consumer-service-fe:3000;
      proxy_http_version 1.1;

      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;

      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
      proxy_read_timeout 600s;
      proxy_send_timeout 600s;
    }
  }
}
